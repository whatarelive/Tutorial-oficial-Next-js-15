import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  // no es posible usarlo en Next js estable por ahora
  // experimental: {
  //   ppr: 'incremental',
  // }
};

export default nextConfig;
